# 环境搭建与快速启动指南

## 视频知识沉淀与智能问答系统

| 文档版本 | 日期 | 作者 | 变更说明 |
|---------|------|------|---------|
| v1.0 | 2026-07-01 | Codex | 初稿完成 |

---

## 1. 系统要求

### 1.1 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 4 核 | 8 核以上 |
| 内存 | 8 GB | 16 GB |
| 磁盘 | 10 GB 可用空间 | 50 GB 以上（视频存储） |
| GPU（可选） | - | NVIDIA GPU >= 6 GB VRAM（加速 Whisper 转写） |

**说明：** 不使用 GPU 也可以运行，但语音转写速度会慢 3-5 倍。

### 1.2 软件要求

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| Python | >= 3.10, < 3.13 | 运行环境 |
| ffmpeg | >= 6.0 | 音视频处理 |
| pip | >= 23.0 | 包管理 |
| Node.js | >= 20，推荐 22 LTS | 前端开发与构建 |
| pnpm | >= 9 | 前端包管理 |
| Rust + Tauri CLI（可选） | stable / Tauri 2 | 桌面端开发与打包 |

**说明：** 只运行 Web 前端时不需要 Rust；执行 Tauri 桌面端调试或打包时再安装 Rust 工具链。

---

## 2. 环境安装

### 2.1 Python 环境

```bash
conda create -n videonote python=3.11
conda activate videonote
# 或 venv
python -m venv venv
# Windows
venv\Scripts\activate
# macOS / Linux
source venv/bin/activate
```

### 2.2 安装 ffmpeg

**Windows：** `winget install ffmpeg` 或手动下载后添加到系统 PATH

**macOS：** `brew install ffmpeg`

**Linux：** `sudo apt update && sudo apt install ffmpeg`

**验证：** `ffmpeg -version`

### 2.3 安装 Python 依赖

```bash
pip install -r requirements.txt
# 或手动安装
pip install fastapi uvicorn[standard] sqlalchemy alembic yt-dlp
pip install httpx pydantic pydantic-settings loguru chromadb faster-whisper
```

**requirements.txt：**

```txt
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
alembic>=1.13.0
yt-dlp>=2024.0.0
httpx>=0.26.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0
chromadb>=0.5.0
faster-whisper>=1.0.0
```

### 2.4 安装前端依赖

```bash
cd frontend
pnpm install
```

---

## 3. 配置 API Key

| 服务 | 用途 | 获取方式 |
|------|------|---------|
| 通义千问 | 笔记生成/问答 | 阿里云百炼平台（新用户免费额度） |
| DeepSeek | 备选 LLM | DeepSeek 开放平台 |
| 必剪 API | 转写备选 | 必剪开放平台 |

**.env 文件：**

```env
TONGYI_API_KEY=sk-xxxxxxxxxxxxxxxx
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
LLM_PROVIDER=tongyi
WHISPER_MODEL_SIZE=medium
WHISPER_DEVICE=auto
```

---

## 4. 运行项目

### 4.1 初始化

```bash
mkdir -p data/videos data/chromadb logs
alembic upgrade head
```

### 4.2 启动后端

```bash
# 开发模式（热重载）
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
# 生产模式
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
```

### 4.3 启动前端

```bash
cd frontend
pnpm dev
```

默认打开 `http://localhost:5173`。如需切换端口：

```powershell
$env:VITE_FRONTEND_PORT=5190; pnpm dev
```

```bash
VITE_FRONTEND_PORT=5190 pnpm dev
```

前端默认把 `/api` 代理到 `http://127.0.0.1:8000`。如需显式指定后端地址，可在仓库根目录或 `frontend/` 下配置：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_FRONTEND_PORT=5173
```

桌面端调试：

```bash
cd frontend
pnpm tauri dev
```

### 4.4 验证

```bash
curl http://localhost:8000/api/v1/system/health
# 浏览器打开 http://localhost:8000/docs
# 前端打开 http://localhost:5173
```

### 4.5 快速端到端验证

```bash
curl -X POST http://localhost:8000/api/v1/videos/parse -H "Content-Type: application/json" -d '{"url":"https://www.bilibili.com/video/BV1xx411c7mD"}'
curl -X POST http://localhost:8000/api/v1/videos/process -H "Content-Type: application/json" -d '{"url":"https://www.bilibili.com/video/BV1xx411c7mD","quality":"720p"}'
curl http://localhost:8000/api/v1/notes/{video_id}
```

---

## 5. 常见问题

| 问题 | 解决 |
|------|------|
| ffmpeg 找不到 | 确认 ffmpeg 在系统 PATH 中 |
| GPU 显存不足 | model_size 改为 small 或 device: cpu |
| 下载失败 | 更新 yt-dlp，请求间隔 >= 10s |
| API 调用失败 | 检查 API Key 和网络连接 |
| 数据库打不开 | 确认 data/ 目录存在且有写权限 |
| 前端白屏 | 确认 `pnpm install` 已完成、开发端口未被占用、浏览器控制台无 Vite 编译错误 |
| 前端接口 404 | 确认后端已启动，并检查 `VITE_API_BASE_URL` 与 `/api` 代理是否指向同一后端 |

---

## 6. Docker 部署

```bash
docker-compose up -d
docker-compose logs -f
docker-compose down
```

> GPU 透传需 `--gpus all`。

---

*文档结束*
